[project]
name = "pacman"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "commentjson>=0.9.0",
    "pydantic>=2.13.5",
]

[tool.pyright]
reportMissingTypeStubs = false
stubPath = "typings"
venvPath = "."
venv = ".venv"